<div class="min-h-screen bg-white font-sans text-slate-900 antialiased">
  <% @landing_page.page_blocks.order(:position).each do |block| %>
    <%= render "landing_pages/blocks/#{block.block_type}", block: block %>
  <% end %>
</div>